[project]
name = "auto-openvpn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-slugify>=8.0.4",
    "selenium>=4.33.0",
    "slugify>=0.0.1",
]
